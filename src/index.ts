/**
 * Punto de entrada de la app de finanzas personales.
 *
 * Todavia no hay features implementadas: el flujo del proyecto (ver CLAUDE.md)
 * arranca en /brainstorming -> /specify, y el codigo se escribe recien en la
 * fase de ejecucion, en TDD. Este archivo existe para que `tsc --noEmit` tenga
 * entradas y el toolchain sea verificable desde el primer commit.
 */
export const APP_NAME = 'mis-finanzas';
